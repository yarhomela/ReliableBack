apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: reliableback
spec:
  type: NodePort    # доступ ззовні для тестування
  selector:
    app: reliableback-api
  ports:
    - port:       80
      targetPort: 8080
      nodePort:   30080